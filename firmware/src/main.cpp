#include <Arduino.h>
#include "config.h"

// Placeholder
void setup() {
  Serial.begin(115200);
  Serial.println("Fall Detection IoT — ESP32");
  Serial.println("Estrutura do projeto criada com sucesso.");
}

void loop() {
  // implementado nas próximas subetapas
}
